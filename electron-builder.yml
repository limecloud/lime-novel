appId: com.limecloud.limenovel
productName: Lime Novel
artifactName: lime-novel-${version}-${os}-${arch}.${ext}
directories:
  output: dist
  buildResources: build
files:
  - out/**/*
  - package.json
asar: true
extraResources:
  - from: apps/desktop/src/renderer/public/logo-lime.png
    to: logo-lime.png
mac:
  category: public.app-category.productivity
  icon: build/icon.png
  target:
    - dmg
    - zip
win:
  icon: build/icon.png
  target:
    - nsis
    - zip
linux:
  category: Office
  icon: build/icon.png
  target:
    - AppImage
    - tar.gz
